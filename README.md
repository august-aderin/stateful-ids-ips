# stateful-ids-ips
Developed a Python-based Intrusion Detection and Prevention System (IDS/IPS) that monitors live network traffic, detects malicious behavior, and automatically blocks offending IP addresses using Windows Firewall.
